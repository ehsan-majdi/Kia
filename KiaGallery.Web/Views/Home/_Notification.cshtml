
@if (Model.Count > 0)
{
    <div class="blinker" style="color:white;position:absolute;top:3px;right:2px;background-color:red;border-radius:100px;width:20px;">
        <span>
            @Model.Count
        </span>
    </div>
}
